import { FiveCommonNamesTeamPipe } from './five-common-names-team.pipe';

describe('FiveCommonNamesTeamPipe', () => {
  it('create an instance', () => {
    const pipe = new FiveCommonNamesTeamPipe();
    expect(pipe).toBeTruthy();
  });
});
