import { SociosPromEdadPerClubPipe } from './socios-prom-edad-per-club.pipe';

describe('SociosPromEdadPerClubPipe', () => {
  it('create an instance', () => {
    const pipe = new SociosPromEdadPerClubPipe();
    expect(pipe).toBeTruthy();
  });
});
